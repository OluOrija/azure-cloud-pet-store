apiVersion: v1
kind: Service
metadata:
    name: projectshadowazurecloudpetstore
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: projectshadowazurecloudpetstore